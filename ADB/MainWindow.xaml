<Window x:Class="ADB_Tool_Automation_Post_FB.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="IKAutomation"
        Width="500" Height="250"
        MinWidth="440" MinHeight="220"
        WindowStartupLocation="Manual"
        Icon="logo_1.ico">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="IKAutomation"
                   FontSize="24"
                   FontWeight="Bold"/>

        <TextBlock Grid.Row="1"
                   Margin="0,8,0,0"
                   Text="Legacy server workflows have been removed. Use Device Diagnostic to verify LDPlayer, capture screenshots, detect game state, and test navigation."
                   TextWrapping="Wrap"
                   Foreground="#FF616161"/>

        <Grid Grid.Row="3" Margin="0,24,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0"
                    Content="Show Log"
                    Click="Button_Click_ShowLog"
                    Height="50"
                    Margin="0,0,6,0"
                    FontSize="15"
                    FontWeight="Bold"
                    Background="#2196F3"
                    Foreground="White"
                    BorderBrush="#2196F3"/>

            <Button Grid.Column="1"
                    Content="IK Device Diagnostic"
                    Click="Button_Click_DeviceDiagnostic"
                    Height="50"
                    Margin="6,0,0,0"
                    FontSize="15"
                    FontWeight="Bold"
                    Background="#7E57C2"
                    Foreground="White"
                    BorderBrush="#7E57C2"/>
        </Grid>
    </Grid>
</Window>
